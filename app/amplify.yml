version: 1
applications:
  - appRoot: app
    frontend:
      phases:
        build:
          commands:
            - npm i
            - npm run build
      artifacts:
        baseDirectory: dist/spa
        files:
          - "**/*"
      cache:
        paths:
          - .npm/**/*
          - node_modules/**/*
