AWS_REGION=us-east-1

# Server Configuration
PORT=3001

# CORS Configuration
# Single origin: CORS_ORIGIN=https://nova.awslearn.cloud
# Multiple origins: CORS_ORIGIN=https://nova.awslearn.cloud,http://localhost:9000
# Allow all (not recommended for production): CORS_ORIGIN=*
CORS_ORIGIN=*
